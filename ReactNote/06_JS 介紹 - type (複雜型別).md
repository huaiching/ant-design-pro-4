# JS 介紹 - type (複雜型別)

1. type 用來定義 複雜的型別，也就是 裡面有多個屬性的型別。

2. 語法：
   
   ```ts
   type 型別名稱 = {
         必填屬性A: 型別;
         選填屬性B?: 型別;           // 選填屬性 用「?」表示
         readonly 屬性C?: 型別;     // 唯獨屬性 只有初始創建時能夠賦值
   }
   ```

---

## 範例

```tsx

```
