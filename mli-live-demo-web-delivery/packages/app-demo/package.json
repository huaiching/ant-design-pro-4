{
  "name": "@mli-csmo/app-demo",
  "version": "0.0.1",
  "description": "No Description",
  "scripts": {
    "analyze": "cross-env ANALYZE=1 max build",
    "start": "cross-env UMI_ENV=dev max dev",
    "build": "max build && yarn copy:CICD",
    "postinstall": "max setup",
    "copy:CICD": "rimraf ../../cicd_build/webroot/demo && cpx \"./dist/**/*\" ../../cicd_build/webroot/demo ",
    "clean:all": "yarn clean:dist && yarn clean:umi",
    "clean:dist": "rimraf ./dist",
    "clean:umi": "rimraf --glob ./src/.umi*",
    "lint": "npm run lint:js && npm run lint:prettier && npm run tsc",
    "lint-staged": "lint-staged",
    "lint-staged:js": "eslint --ext .js,.jsx,.ts,.tsx ",
    "lint:fix": "eslint --fix --cache --ext .js,.jsx,.ts,.tsx --format=pretty ./src ",
    "lint:js": "eslint --cache --ext .js,.jsx,.ts,.tsx --format=pretty ./src",
    "lint:prettier": "prettier -c --write \"**/**.{js,jsx,tsx,ts,less,md,json}\" --end-of-line auto",
    "tsc": "tsc --noEmit"
  },
  "lint-staged": {
    "**/*.less": "stylelint --syntax less",
    "**/*.{js,jsx,ts,tsx}": "npm run lint-staged:js",
    "**/*.{js,jsx,tsx,ts,less,md,json}": [
      "prettier --write"
    ]
  },
  "dependencies": {
    "@mli-csmo/app-common": "0.0.1",
    "@mli-csmo/app-model": "0.0.1"
  }
}
