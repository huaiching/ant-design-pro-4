{
  "name": "@mli-csmo/app-common",
  "version": "0.0.1",
  "main": "src/index.ts",
  "typings": "src/index.d.ts",
  "license": "MIT",
  "files": [
    "src/*"
  ],
  "scripts": {
    "start": "yarn tsc && babel src -d target --config-file ./.babelrc --extensions \".js,.jsx,.ts,.tsx\" --watch && yarn copyFiles",
    "build": "yarn tsc && babel src -d target --config-file ./.babelrc --extensions \".js,.jsx,.ts,.tsx\" && yarn copyFiles",
    "tsc": "tsc --declaration --outDir target/ --emitDeclarationOnly --allowJs false --checkJs false",
    "copyFiles": "cpx \"./src/**/*.{json,less,ico,html,gif,svg,png,ttf,TTF,woff,woff2,js,jsx,css}\" ./target/",
    "clean:all": "rimraf ./target"
  },
  "dependencies": {
    "@types/js-cookie": "^3.0.6",
    "cpx": "^1.5.0"
  },
  "sideEffects": [
    "**/*.less"
  ]
}
